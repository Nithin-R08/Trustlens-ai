# Local development
NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:8000

# Keep this disabled on Vercel unless you specifically want the Spline runtime package.
# The iframe fallback is more reliable for deployment.
NEXT_PUBLIC_ENABLE_SPLINE_RUNTIME=0